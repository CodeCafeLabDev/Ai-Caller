
"use client";

// This component is being reverted to an empty state as its functionality
// is moved back to the /ai-templates/create page.
// The form logic previously here is now in src/app/(app)/ai-templates/create/page.tsx

import * as React from "react";

export function CreateTemplateForm() {
  return (
    <div>
      {/* Content for this component has been moved to /ai-templates/create page. */}
      {/* This file can be removed or repurposed if no longer needed by the sheet system. */}
    </div>
  );
}

export type CreateTemplateFormValues = {
  // Define a minimal type or leave empty if the component is truly unused.
  templateName?: string;
};

    